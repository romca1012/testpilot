## Lot 12 — Valeurs de champ observées (masque de saisie, références inexistantes) — terminé, sous réserve de la revue et de ta décision de fusion

PR : n° 6 (`lot-12-dev` → `master`, brouillon). Branche de lot conservée après fusion ; hash d'origine cités ci-dessous.

Décisions utilisées : D10 bis (sonde de saisie avec garde-fous), D11 (références inexistantes), validées par le porteur le 2026-09-24.

Changements :
- `src/testpilot/connectors/_sonde_saisie.py` (nouveau) — sonde de saisie sans soumission : page jetable du contexte authentifié, écritures réseau interceptées et abandonnées (`page.route`), aucune sonde back-office ni sous `ODOO_ENV=prod`, recherche bornée d'un exemple valide et stable ; un champ qui refuse la saisie (`type=number`) ou qui échoue ne fait plus tomber le formulaire.
- `src/testpilot/connectors/_web_helpers.py`, `generic_web.py`, `odoo.py`, `base.py` — extraction des attributs de masque, appel de la sonde (portail seulement côté Odoo), `name_search` RPC.
- `src/testpilot/generation/formats_observes.py` (nouveau) — bloc « Formats de saisie OBSERVÉS » injecté dans l'observation, plafonné, texte de l'application cité comme donnée.
- `src/testpilot/generation/references.py` (nouveau), `tools/write.py`, `smoke_check.py` — D11 : refus bloquant d'une référence prouvée inexistante, contrôle détectif `catalogue:`.
- `src/testpilot/generation/tools/inspect.py`, `tools/__init__.py`, `agent.py` — options de liste, champs relationnels, trace compacte de la sonde dans les preuves.
- `behave_runtime/steps_library/_base_helpers.py`, `regles_apprises.py` — `valeur_retenue` structurée, sans lecture de texte libre.
- `src/testpilot/generation/prompts/system_prompt.md` — règle 7 « un format de saisie s'OBSERVE » (+1164 octets).
- `scripts/qualify_campaign_pilot.py` — contrôle préalable, secret API absent des artefacts, arrêt réel sur plafond de coût, mode `--explorer-urls`.
- `frontend/src/components/ReviewGate.vue` — familles `message_non_observe` et `produit_non_observe` libellées.
- `.gitattributes` — LF sur `frontend/index.html` et les prompts (hash de provenance et CSP en dépendent).
- `CLAUDE.md` — `master` protégé : plus de push direct, PR + CI verte, accord du porteur avant fusion.

Tests ajoutés :
- `tests/test_sonde_saisie.py` — garde principal (POST/PUT/PATCH/DELETE interrompent), aucun clic ni navigation, aucune sonde back-office ni en prod, page jetable fermée sans `beforeunload`, exemple stable pour masques de 7/4/2, `type=number` refusé sans faire tomber le formulaire (doublure + Chromium réel `ebc8d46`, échoue sans le correctif), Chromium réel : fetch/sendBeacon/XHR PUT → zéro écriture reçue, masques jamais vus.
- `tests/test_formats_observes.py`, `test_references_inexistantes.py`, `test_valeur_retenue_structuree.py`, `test_smoke_check.py`, `test_campagne_isolation.py` — bloc plafonné et cité comme donnée, refus D11 jamais pour une valeur créée plus tôt, valeur retenue hors clé d'identité, arrêt sur plafond (code 3), clé jamais copiée.

Critères d'acceptation :
- [x] La sonde n'écrit rien sur le serveur (test réel Chromium, 3 vecteurs) ; réexploration : 0 création métier sur 420 modèles.
- [x] Aucune sonde sur le back-office Odoo ni sous `ODOO_ENV=prod`.
- [x] Les valeurs observées atteignent la génération ; les références inexistantes sont refusées à l'écriture.
- [x] Mesure avant/après par cas séparés (conception / cas 13 / masques non vus), 3 tirages, coût sous 1 €/cas.
- [x] CI verte sur le dernier commit poussé (`27cc47c`, puis `dfb6143` : 4 checks requis verts) ; conformance 5 passed sur `27cc47c`.
- [ ] Suite pytest complète locale sur le commit final : non lancée (décision du porteur : la CI de la PR fait foi).

Mesures : voir `docs/mesures/lot12-avant-apres-2026-09-24.md`.
- Coût : 1,500 $ → 1,255 $ (0,125 → 0,105 $ / essai) ; itérations de l'agent 5,33 → 4,08.
- `conforme` au premier passage : 2/12 → 9/12 ; `donnee_invalide` 5 → 0 ; `technical_error` 1 → 0 ; `non_conforme` 4 → 3 (cas 99 seul).
- Masques non vus : téléphone et carte trouvés, IBAN à préfixe de lettres non trouvé (limite assumée).
- Commits : réexploration (2ᵉ passage) et campagne sur `27cc47c` (worktree `testpilot-lot12`). Le 1ᵉʳ passage de réexploration, sur `8339179`, a révélé le défaut `type=number`.

« Sauvegarde automatique détectée » : 2 essais sur 12 (cas 99 tirage 2, cas 101 tirage 1), raison `POST /web/dataset/call_kw/res.users/read`. Aucun formulaire marqué à la réexploration.

Écarts constatés avec le plan :
- La sonde ne s'exécute pas dans le crawl de l'annuaire mais à chaque `inspect_page_form` : chaque génération de la mesure « après » réexplore. Un mode `--explorer-urls` a été ajouté pour la réexploration dédiée.
- Pages `/web` du projet 12 : la clé `sonde` est absente (pas « ignoree »), la sonde n'étant appelée que sur la branche portail d'Odoo.
- Prompt : base mesurée avec fins de ligne CRLF, « après » en LF (confond de taille, pas de contenu).

Risques / points à surveiller :
- **Faux positif de la détection** : `res.users/read` est une lecture RPC émise en `POST`, non couverte par `_ARRIERE_PLAN`. Effet : sonde abandonnée (sûr), format perdu sur 2 essais/12. Correctif proposé, non appliqué pour ne pas invalider la mesure : liste blanche des méthodes de lecture de `/web/dataset/call_kw/<modèle>/<méthode>` (`read`, `search_read`, `web_read`, `web_search_read`, `search_count`, `fields_get`).
- Attribution de l'amélioration non séparée (sonde, bloc de formats, D11, prompt) ; 3 tirages par cas ; un `conforme` sur chemin négatif prouve qu'une erreur s'affiche, pas que c'est le champ visé.
- `ebc8d46` (test seul) et `dfb6143` (CLAUDE.md) suivent le dernier commit de code mesuré (`27cc47c`) ; la CI de `dfb6143` est verte.
- Le ruleset de `master` est actif (id 23929449) : cette PR doit passer les 4 checks requis, en « rebase and merge ».

Suggestions hors périmètre :
- Lot 02 : constats F2 (assertion d'environnement inventée) et F10 (refus silencieux → `indetermine`, jamais `non_conforme`).
- Rendre l'assertion de chemin négatif discriminante (désigner le champ à l'origine de l'erreur).
- Sonde : IBAN à préfixe de lettres (alphabet adaptatif).
