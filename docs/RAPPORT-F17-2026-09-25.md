## Mini-lot F17 (+ F21) — la navigation par menu Odoo sur Community, puis la connexion du navigateur — **en attente de validation Enterprise sur Sapian** (pas « terminé » : marqué ainsi au tableau de suivi du plan)

Décisions utilisées : aucune `D#`. Mini-lot demandé par le porteur le 2026-09-24 (« F13 » dans les échanges ; l'identifiant F13 était déjà pris au registre par la
sonde du lot 12 : ce constat est **F17** au plan), priorité avant 07b-e, taille S. **F21** y a été ajouté le 2026-09-25 sur demande explicite du porteur.

Analyse et recommandation avant tout code : voir la réponse du 2026-09-24 (sondage de `/web#action=menu` sur 16.0, 17.0 et 18.0 Community).

Changements :
- `behave_runtime/steps_library/_base_helpers.py`
  - **F17** — `_ouvrir_grille_applications` : `/web#action=menu` est le home menu d'**Enterprise** ; sur Community (16.0, 17.0, 18.0) elle ne rend aucune grille
    (boîte « Erreur Odoo » sur 16.0, « L'action 'menu' n'existe pas » sur 17.0 et 18.0). UNE attente (15 s, marge historique) sur « la grille `.o_app` OU le commutateur
    `.o_navbar_apps_menu button` » : la grille → Enterprise, comportement d'origine, aucun clic ni navigation ; le commutateur → Community : on repart de `/web` (la boîte d'erreur
    modale recouvre le commutateur), on le clique jusqu'à 4 fois (un clic donné avant l'attache des gestionnaires n'ouvre rien) et on attend la grille. Ne lève jamais.
  - **F21** — `connecter_le_navigateur_si_page_vide` : une seule fonction, appelée par `navigate` ET `navigate_menu`, qui connecte le navigateur quand la page est `about:blank`.
- `tests/test_odoo_steps_login_delegue.py` — la doublure `_FakePageMenu` démarre sur une session déjà ouverte (elle était sur `about:blank`, ce que F21 change) ; assertions inchangées.
- `pyproject.toml`, `.github/workflows/banc.yml` — marqueur `banc` (exclu de `pytest -q`), étape de test sur le banc, déclencheurs.
- Plan : F17 traité, F18-F20 (ex-F14/F15/F16 du banc) et F22-F24 consignés comme constats séparés.

Tests ajoutés :
- `tests/test_navigate_menu_communaute.py` (12) — l'ordre et les bornes de F17 sur doublures (Enterprise : aucun clic ni navigation ; Community : racine puis commutateur, réessai, plafond de 4 ; ni l'un ni l'autre :
  une seule attente de 15 s) ; **F21** : une page vide est connectée AVANT d'ouvrir le menu, une page ouverte ne l'est pas, `navigate` et `navigate_menu` partagent le même garde ; le seul sélecteur de repli est celui de Community.
- `tests/test_navigation_menu_banc.py` (`-m banc`, vraies instances) — la route historique ne rend aucune grille ; `navigate_menu` atteint la liste des devis ; **avec l'ancien comportement la même navigation échoue** (falsifiabilité) ;
  et pour F21, sur un navigateur vierge : atteint la liste, et échoue si on retire le garde.

Preuves :
- **F17, trois versions Community** : 3/3 tests du banc sur 16.0, 17.0 et 18.0 (avant le second correctif de session froide) ; sur 17.0, 5/5 avec F21.
- **Session froide (sous-processus Behave réel, sans LLM)** : « Ventes / Commandes / Devis » et « Achats / Commandes / Demandes de prix » atteignent la liste. « Projet / Projets » atteint bien la page (vue kanban par défaut : mon assertion sur une vue liste était fausse, pas la navigation).
- **F21** : le même scénario SANS le step « je me connecte » échoue (`Timeout … waiting for get_by_text("Ventes")`, 23 s) avant, passe après.

**Vérification faite, rien trouvé de plus (demandée par le porteur — pas supposée)** : inventaire par AST de toutes les fonctions de la bibliothèque qui OUVRENT une page (`goto`) : `navigate` (garde), `navigate_menu` (**seul manquant**, corrigé),
`step_navigate_url` (garde), `step_login_portal` (se connecte lui-même), `_ouvrir_application` du connecteur web (connexion faite par son appelant ; « sans me connecter » est volontairement sans connexion), et le repli de F17 (déjà connecté).
Les helpers d'interaction (`fill_field`, `click_button`, `select_*`…) n'ouvrent aucune page : ils agissent sur la page courante, donc dépendent d'une navigation préalable — aucun des 15 cas de la campagne n'a échoué sur `about:blank`
(vérifié dans les journaux d'exécution). Aucun autre correctif de cette famille.

Mesures — génération réelle sur Odoo 17.0, 15 cas, mêmes specs, exécution physique unique (`docs/mesures/banc-17.0-2026-09-2*-generation*.md`) :

| Mesure | I3 (1er passage) | I4 (verdict exploitable) | I6 ($/cas) | Coût |
|---|---|---|---|---|
| Avant tout (lot 04, 2026-09-24) | 20 % (3/15) | 20 % | 0,124 | 1,86 $ |
| F17 v1 (première version du correctif) | 33 % | 27 % | 0,118 | 1,77 $ |
| F17 v2, sans F21 | 27 % | 20 % | 0,121 | 1,81 $ |
| F17 + F21, **crédits API épuisés en cours de route** (cas 14 et 15 : erreur 400, comptés à tort en échecs de génération) | 40 % | 40 % | 0,113 | 1,69 $ |
| **F17 + F21, crédits rechargés — RÉFÉRENCE I3 avant 07b-e** | **33 % (5/15)** | **33 % (5/15)** | **0,101 $** | 1,51 $ |

La mesure précédente était **contaminée** : deux essais avaient échoué faute de crédits, pas à cause de l'agent. Elle est archivée (`…-tentative3-credits-epuises`) et non retenue. Les échantillons sont de 15 cas et l'agent est non
déterministe : les écarts entre tentatives contiennent du bruit d'agent. **La mesure qui fait foi est la dernière ; à ne comparer qu'à elle-même avec le même corpus.**
**Coût cumulé réel du mini-lot : 6,78 $** (1,77 + 1,81 + 1,69 + 1,51) — bien au-delà des ≈ 2 $ annoncés : deux défauts distincts (F17, F21) découverts par les relances successives, puis une mesure faussée par les crédits.

Répartition de la référence : 5 `conforme`, 9 `technical_error`, 1 génération échouée (aucune faute de crédits). **Plus aucun cas ne tombe sur la navigation par menu** (0 timeout `get_by_text` de menu contre 8 avant F17).

**Ce que la référence dit** : I3 ne monte pas (33 % contre 20 %) parce que le goulot s'est déplacé — la navigation est réglée, les 9 erreurs restantes sont des gestes d'interface Odoo que l'agent réécrit à la main et rate (ligne de commande / facture,
clic sur un bouton d'enregistrement inexistant, sélection d'un many2one, champ « vidé » par `fill("")`), plus F18 et F22. C'est le périmètre du lot 08b (vocabulaire de gestion) — le mini-lot F17 n'en est pas responsable.

Constats **séparés**, consignés au plan (aucun n'est de la famille F21, « une page ouverte sans session navigateur ») :
- F18 : sérialisation des factures par odoorpc/Odoo 17 (1 cas).
- F22 : mot de passe d'un compte secondaire (`banc_commercial`) inventé par l'agent — connexion RPC refusée (1 cas). À relier à D8 / lots 07b et 08d (comptes multiples).
- F23 : interactions d'interface réécrites par l'agent et ratées (6 cas : `fill("")` sur many2one, boutons d'enregistrement inventés, sélection many2one, ligne de commande/facture) ; même famille que F20 (l'agent invente ce qui n'existe pas). Lot 08b.
- F24 : un cas sans enregistrement en contexte (`RuntimeError`, agent) et, dans la mesure précédente, un `non_conforme` sur l'instance saine (assertion sur un enregistrement jamais créé : faux FAILED d'origine agent).
- 1 génération échouée, non instruite.

Écarts constatés avec le plan :
- Le premier correctif de F17 (sonde du commutateur après 2,5 s) échouait sur le banc : le clic tombait sur la boîte d'erreur de l'ancienne route ; puis, en session froide, le commutateur n'était pas encore là. Deux corrections, dites ici parce que l'ordre « ancienne route d'abord, repli ensuite » demandé par le porteur a dû devenir « une attente sur grille OU commutateur ».
- **Le comportement Enterprise n'est pas vérifiable localement** : aucune instance Enterprise à disposition. Les tests sur doublures fixent qu'il est INCHANGÉ (une attente de 15 s sur la grille, aucun clic, aucune navigation, et le sélecteur de repli n'existe que sur Community), mais rien ne le prouve sur une vraie instance.
  **À valider sur Sapian avant de considérer F17 définitivement clos.**

Risques / points à surveiller :
- Community : le clic sur le commutateur s'appuie sur un sélecteur d'interface (`.o_navbar_apps_menu button`), à revalider à chaque version d'Odoo (le banc le fait sur 16.0, 17.0, 18.0 à chaque exécution du workflow).
- `connecter_le_navigateur_si_page_vide` connecte avec les identifiants du projet : un cas qui teste la connexion Odoo elle-même n'a pas d'équivalent de « sans me connecter » côté Odoo (existe seulement pour le connecteur web).
- La référence I3 (33 %) reste loin de la cible (≥ 85 %) : l'écart est maintenant dû aux gestes d'interface écrits par l'agent et à d'autres défauts de bibliothèque (F18, F22-F24), plus à la navigation.

Plafond de coût (demande du porteur, 2026-09-25) — **vérification faite** : le garde-fou existe déjà et agit EN AMONT. `qualify_campaign_pilot._boucle_essais` s'arrête AVANT le premier essai qui pourrait dépasser
le plafond (`_raison_d_arret`, code de sortie 3, testé : `test_aucun_essai_n_est_lance_au_dela_du_plafond`) ; `banc_mesure.py --mode génération` exige `--plafond-cout` (`parser.error` sinon, testé) et le transmet. Le plafond
de 3 $ a été appliqué à chaque relance (coûts réels 1,5 à 1,8 $). **Manque trouvé et corrigé avant la prochaine campagne** : une erreur de crédits n'arrêtait pas la campagne et les essais refusés étaient comptés comme des
générations échouées (cas 14 et 15 de la 3e relance). Maintenant : arrêt immédiat, essai non retenu, mesure marquée « INCOMPLÈTE » dans le rapport (3 tests). Limite assumée : le plafond se juge entre deux essais.
Rappel opérationnel ajouté au plan (§3, règles de mesure).

Suggestions hors périmètre :
- Lister dans le prompt les attributs et API réellement disponibles (`context.*`, Playwright) : inventions de l'agent dans la référence (F20, F23).
- Un step Odoo « je me connecte en tant que "<compte>" » avec les comptes du projet (lot 08d).
