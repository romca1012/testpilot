## Lot 04 — Banc de mesure : instance Odoo de référence, défauts et pannes injectés, indicateurs I1-I6 — terminé (sous réserve de la relecture des attendus, de la revue et de la CI)

Décisions utilisées : **D9** (validée le 2026-09-24 par le porteur : Odoo 16.0 / 17.0 / 18.0 Community, données de démo,
modules `sale_management`, `purchase`, `stock`, `account`, `crm`, `project` ; helpdesk est Enterprise, exclu).

**Statut.** Tout est livré et mesuré, mode figé (I1, I2, I5, sur les 3 versions) **et** mode génération (I3, I4, I6, sur 17.0 :
lancé le 2026-09-24 avec l'autorisation du porteur et un plafond de 5 $ ; coût réel 1,86 $). Les attendus (`specs/banc/attendus.yaml`)
sont écrits par moi et **restent à relire par le porteur**.

Changements :
- `compose.banc.yml`, `banc/images.env` — PostgreSQL 16 + Odoo, **images figées par digest** (postgres 16, odoo 16.0 / 17.0 / 18.0),
  port dédié 18069 lié à `127.0.0.1`, volumes jetables (`down -v`).
- `scripts/banc_init.sh` et `.ps1` — base `banc` avec données de démo, modules de D9 + `tp_bugs_injectes`, `odoo neutralize`, comptes
  `banc_commercial` (Ventes / Utilisateur) et `banc_manager` (Ventes / Administrateur + Stock + Facturation), `fr_FR` pour tous.
- `banc/odoo_addons/tp_bugs_injectes/` — 7 défauts (`vente_sans_livraison`, `total_faux`, `client_non_requis`, `etat_bloque`,
  `facture_non_postee`, `droit_trop_large`, `message_absent`), activables un par un par `ir.config_parameter` `tp_bug.<code>` = `1`,
  **inactifs par défaut**, compatibles 16.0 / 17.0 / 18.0 ; 8 tests Odoo (un par défaut : activé → l'effet, désactivé → rien ne change).
- `banc/cas_figes/` (15 cas, feature + steps écrits à la main, tenant lieu de versions approuvées) et `specs/banc/*.md` (15 specs) :
  vente, achat, stock, facturation, CRM, projet, droits d'accès, champ requis, portail, comptage cloisonné (lot 01), et **deux cas F7**
  (clic d'onglet d'un formulaire, vrai refus de sauvegarde) réclamés par le plan pour valider le lot 11 sur 16.0 / 17.0 Community.
- `specs/banc/attendus.yaml` — statut attendu de chaque cas : instance saine, sous chaque défaut concerné, sous chaque panne.
- `scripts/banc_mesure.py` — `--mode figé` (défaut, coût nul) et `--mode génération` (plafond de coût obligatoire) ; I1-I6, « non mesuré ≠ zéro »,
  code de sortie 1 sur faux PASSED ou blocage mal attribué, refus de toute instance non locale, données jetables ; sortie Markdown + JSON.
- `scripts/banc_projet.py` — projet « Banc Odoo <version> » (idempotent), exploration, annuaire versionné ; `scripts/banc_generation.py` —
  mode génération (réutilise `qualify_campaign_pilot.py` sur un dossier de données jetable).
- `.github/workflows/banc.yml` — nocturne + manuel + PR qui touche le banc ; matrice 16.0 / 17.0 / 18.0 ; mode figé seulement ; échec si I1 > 0
  ou I5 < 100 %.
- `banc/README.md`, plan (suivi, registre F17-F19), mesures dans `docs/mesures/banc-*`.

Tests ajoutés :
- `tests/test_banc_mesure.py` (25) — chaque indicateur sur des résultats simulés ; « non mesuré » ≠ zéro ; un cas non mesuré sort du dénominateur ;
  **un faux PASSED et une panne mal attribuée font échouer le code de sortie** ; un faux FAILED seul non ; rien de mesuré → code 3 ; attendus bien
  formés et un défaut du module sans attendu détecté ; instance non locale refusée ; orchestration (sain → défauts → pannes, remise en état même
  si un cas plante, panne non posée = non mesuré).
- `tests/test_banc_generation.py` (24) — les 15 specs se lisent en document métier sans LLM ; réduction de `campagne.json` en I3/I4/I6 ; projet du banc
  idempotent, chiffré, refuse une instance non locale.
- `banc/odoo_addons/tp_bugs_injectes/tests/test_bugs.py` (Odoo, 8) — 8/8 sur 16.0 et 18.0 en local ; 17.0 : 8/8 avant l'ajout de `pricelist_id`
  (nécessaire pour 16.0, sans effet sur 17.0), à confirmer par la CI.

Critères d'acceptation :
- [x] `docker compose -f compose.banc.yml up -d` + `banc_init` aboutissent sur 17.0 en < 10 min : **7 min** (images déjà tirées ; 16.0 : 2 min 13,
      18.0 : 2 min 36 — mêmes conditions).
- [x] Mesure initiale publiée, mode figé **et** mode génération : figé sur les 3 versions, génération sur 17.0 (plafond 5 $, coût réel 1,86 $) ; mesure aussi sur
      le commit qui précède les lots 01-03 : faite (ci-dessous).
- [ ] Workflow `banc.yml` vert sur au moins une version : écrit ; **sa première exécution est celle de la PR** (voir CI dans le message de fin).
- [x] `python -m pytest -q` et ruff critique verts : 2511 passés, 16 ignorés (10 min 54) ; ruff critique vert ; Vitest non concerné (aucun changement frontend).

Mesures (mode figé, 15 cas, 9 paires défaut/cas, 7 paires panne/cas, échantillon : 31 observations par version ; instance saine sans défaut actif) :

| Version | I1 faux PASSED | I2 faux FAILED | I5 blocage attribué | Écarts aux attendus |
|---|---|---|---|---|
| 16.0 | 0 / 9 (0 %) | 0 / 15 (0 %) | 7 / 7 (100 %) | 0 |
| 17.0 | 0 / 9 (0 %) | 0 / 15 (0 %) | 7 / 7 (100 %) | 0 |
| 18.0 | 0 / 9 (0 %) | **2 / 15 (13,3 %)** | 7 / 7 (100 %) | 2 (`ui_champ_requis`, `ui_refus_sauvegarde`) |

**Mode génération, 17.0, instance saine** (15 cas générés depuis `specs/banc/*.md`, une génération par cas, exécution physique unique, aucun rejeu ni réparation ; `docs/mesures/banc-17.0-2026-09-24-generation.md`) :

| Indicateur | Valeur | Échantillon | Cible |
|---|---|---|---|
| I3 — exécution au 1er passage | **20 %** (3 / 15) | 15 cas générés | ≥ 85 % |
| I4 — verdict exploitable | **20 %** (3 / 15) | 15 cas générés | ≥ 75 % |
| I6 — coût par nouveau cas | **0,124 $** | 15 cas, 1,86 $ au total | < 1 € |

Répartition : 3 `conforme` (`crm_opportunite`, `portail_acces`, `vente_total`), **10 `technical_error`**, 2 cas dont la génération a échoué (`ui_refus_sauvegarde`,
`vente_comptage`). Causes lues dans les journaux d'exécution : **8 cas sur 10 échouent au step `je navigue vers le menu Odoo`** que l'agent choisit dans le
catalogue (`Timeout … waiting for get_by_text("Ventes"|"Achats"|"Projet")`) — c'est **F17**, le défaut de la bibliothèque trouvé par le banc, qui explique à lui seul la
plus grande part de l'écart à la cible (sans lui, jusqu'à 11 cas sur 15 auraient pu atteindre l'exécution : non mesuré, à refaire après correction) ; 1 cas échoue sur F18 (`frozendict`, RPC odoorpc écrit par l'agent) ; 1 cas sur un attribut inventé (`context.odoo_host`).
**Aucun faux verdict** : sur les 3 cas exécutés jusqu'au bout, les 3 sont `conforme` sur l'instance saine (0 faux FAILED). La cible I3 ≥ 85 % est très loin ; le levier
identifié est F17, pas l'agent.

**L'effet des lots 01 à 03 (et 07a), sur 17.0** — mêmes 15 cas, même instance, code de `42fa613` (le commit qui précède le lot 01) contre le code
actuel ; les cas de `42fa613` sont rejoués avec une doublure de `constater` (assertion nue) et de `PreconditionNonRemplieError` (exception ordinaire),
qui n'existaient pas encore :

| Indicateur | Avant les lots (`42fa613`) | Après (ce lot) |
|---|---|---|
| I1 faux PASSED | 0 / 8 | 0 / 9 |
| I2 faux FAILED | 0 / 13 | 0 / 15 |
| **I5 blocage attribué** | **0 / 7** (pannes en `retest`, et `module_desinstalle` en **`non_conforme`** — la panne d'environnement accusait l'application, c'est F2) | **7 / 7** (`blocked`, cause `precondition_non_remplie`) |

(Avant, le corpus avait 13 cas ; les deux cas F7 ont été ajoutés ensuite.) **I1 ne bouge pas** : les défauts injectés se voient par des lectures RPC directes,
que ni le cloisonnement des comptages (lot 01) ni la preuve de constat (lot 03) ne changent. Le banc ne prouve donc PAS que ces lots ferment leurs
faux PASSED — F1 (création concurrente pendant le scénario) et D3 (assertion jamais exécutée) ne sont pas exercés par les 7 défauts. À ajouter (voir Suggestions).

Ce que le banc a déjà trouvé (**constats sur la bibliothèque de steps, non corrigés ici — hors périmètre**) :
1. **F17 — `je navigue vers le menu Odoo` échoue sur Odoo 17.0 Community** : le step ouvre `/web#action=menu`, qui n'existe pas (« L'action 'menu' n'existe pas »).
   Le cas `ui_champ_requis` navigue donc par l'URL de l'action. Mesuré sur 17.0 ; 16.0 et 18.0 non sondés pour ce step.
2. **F18 — les steps de lecture `le champ … est égal à` échouent sur une facture (17.0)** : `browse(id).read(...)` d'odoorpc envoie un `context` avec lequel Odoo 17
   échoue à sérialiser la réponse (`keys must be str … not frozendict`) ; `execute_kw` sans contexte fonctionne. `facture_validation` lit donc au niveau de la classe.
3. **F19 — `une notification d'erreur de validation est affichée dans l'interface Odoo` ne voit rien sur Odoo 18.0** : le sélecteur
   `.o_notification.border-danger` n'existe plus (la barre rouge est un `span.o_notification_bar.bg-danger`). **Faux FAILED** sur 18.0 : I2 = 13,3 %.
4. La désinstallation réelle d'un module par RPC (`button_immediate_uninstall`) dépasse le délai, laisse le module en `to remove` et supprime des
   données : la panne « module désinstallé » est donc **simulée par l'état du module** (`ir.module.module.state`), que le step de Contexte lit.

**Validation de F7 (lot 11) sur Odoo Community** — le plan la réclamait sur le banc : sur **16.0 et 17.0 Community** (et 18.0), avec un scénario Behave complet :
un clic sur l'onglet « Autres informations » d'un formulaire (l'URL ne change pas) **ne déclenche aucun faux `donnee_invalide`** (`ui_onglet_formulaire` : `passed`
sur les trois versions) ; un vrai refus de sauvegarde sans client (`ui_refus_sauvegarde`, l'URL ne change pas) est vu, sans être pris pour une donnée fautive
(`passed` sur 16.0 et 17.0 ; sur 18.0 il échoue à cause de F19, pas de F7).

Écarts constatés avec le plan :
- **Mode figé : des cas « déjà approuvés » n'existent pas pour un banc neuf** : ils sont écrits à la main (feature + steps) et tiennent lieu de versions approuvées —
  la commande le laissait implicite. Ils passent par les steps de la bibliothèque et `constater`, comme un cas généré après le lot 03.
- **Panne « module désinstallé » simulée par l'état** (ci-dessus), pas par une désinstallation physique.
- **Ajout non demandé** : le déclencheur `pull_request` (chemins du banc) de `banc.yml`, pour qu'un banc cassé se voie avant la nuit suivante.
- **Un module Odoo non trivial à porter** : `partner_id` est `NOT NULL` en base et Odoo remet la contrainte à chaque mise à jour ; le défaut `client_non_requis`
  relâche donc le champ (et `partner_invoice_id`, `partner_shipping_id`, `pricelist_id`) au niveau du modèle et rend l'obligation d'origine en Python et dans
  `fields_get` tant que le défaut est inactif — vérifié par les tests du module.

Revue `verdict-reviewer` : verdict BLOQUANT sur deux points, corrigés — (1) **le code de sortie laissait le job vert quand I1 ou I5 n'avait aucune observation** (docker absent,
Odoo tombé…) tant qu'un autre indicateur était mesuré : I1 et I5 sont maintenant des PORTES (code 3 si l'un est non mesuré, hors `--configs`), et un défaut dont un cas sort
`blocked`/`retest` (ni détecté ni manqué) donne le code 1 ; (2) **PyYAML n'était déclaré nulle part** (le workflow et les tests auraient cassé sur un environnement propre) : ajouté aux extras `dev`.
Corrigés aussi : `ODOO_ENV=prod` est REFUSÉ (il était supprimé, ce qui neutralisait le garde de production), le banc vérifie que la base s'appelle `banc` et que `tp_bugs_injectes`
est installé avant de toucher aux défauts (127.0.0.1 peut être un tunnel), et un module laissé `uninstalled` par un run interrompu est remis en état au départ. 7 tests ajoutés.

**Ce que « job vert » veut dire.** Le workflow est vert quand I1 = 0 et I5 = 100 % **et** que ces deux portes ont été mesurées. **I2 n'est pas une porte** : sur 18.0, le job sera vert avec I2 = 13,3 %
(F19). Un job vert n'est pas « la fiabilité est bonne » : le tableau de mesure, publié en artefact, se lit toujours. La mesure « avant » (`42fa613`) reconstruit le corpus avec des doublures
(`constater` = assertion nue, `PreconditionNonRemplieError` = exception ordinaire) : le contraste I5 0/7 → 7/7 mélange code de bibliothèque réel et doublures de cas — ce n'est pas un historique réel.

Risques / points à surveiller :
- **Exception à la règle « attendu écrit avant tout run »** : les lignes `sain` de `ui_onglet_formulaire` et `ui_refus_sauvegarde` (2e commit du fichier) ont été écrites après avoir vu ces deux cas passer sur l'instance saine ; le reste du fichier a été écrit avant tout run d'un cas. Notée aussi dans `attendus.yaml`.
- **Les attendus sont de ma main** (règle du lot : jamais déduits d'un run, écrits avant le premier run) mais **à relire par le porteur**. Un attendu faux
  fausserait I1 et I2 sans qu'aucun test ne le voie.
- **I1 = 0 ne prouve pas l'absence de faux PASSED** : il prouve que ces 7 défauts sont vus par ces 15 cas. F1 concurrent et D3 ne sont pas couverts.
- **La panne « Odoo arrêté »** pilote Docker : elle échoue (et est marquée non mesurée) si `docker` est absent ; le module de panne remet toujours Odoo en marche.
- **Mode génération : un seul passage, une seule version** (17.0, 15 essais) — un échantillon de départ, pas une mesure stable ; la clé LLM a dû être fournie par
  l'environnement (le worktree n'a pas de `.env`). Le rapport de sortie était nommé avec un accent (`génération`) : corrigé en `generation`.
- **Images figées par digest** : un digest peut disparaître du registre ; le mettre à jour est un commit dédié (`banc/images.env`).
- **Durée** : ≈ 7 min par version en mode figé, ≈ 3 min d'init ; la CI (3 versions) tient largement dans le plafond de 60 min par job.

Ce qui reste (à décider par le porteur) :
1. **Relire `specs/banc/attendus.yaml`.**
2. Décider de la suite : **corriger F17 avant les lots suivants** (le banc mesure I3 à 20 % surtout à cause de lui) ; et si le banc doit couvrir F1 concurrent et D3 (Suggestions).

Suggestions hors périmètre :
- Ajouter au banc une **perturbation de création concurrente** (un tiers crée un enregistrement entre le relevé et le contrôle d'un cas de comptage) et un **cas dont
  l'assertion n'est jamais exécutée**, pour que I1 exerce F1 et D3 ; aujourd'hui aucun défaut du banc ne le fait.
- Corriger F17, F18, F19 (`_base_helpers.py`) — relèvent du lot 08 (Odoo ERP) ; F19 est le plus urgent (faux FAILED en 18.0).
- Publier I1-I6 dans `docs/QUALITE-GENERATION-*.md` à chaque lot, comme le plan le prévoit.
